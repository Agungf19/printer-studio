[project]
name = "scanpilot"
version = "0.2.0"
description = "Desktop scanner, compression, OCR, and document export app."
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["backend"]

[tool.ruff]
line-length = 100
